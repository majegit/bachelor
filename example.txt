function main(){
    int i = 1;
    char firstLetterOfAlphabet = a;
    double name = 1.1;

    while(i > 10 ){
        name = name * i;
        if(name > 15 ){
            i = something(name);
        } else {
            i = somethingElse(name);
        }
        i++;
    }
    print(i);
}

function something(double asd ){
    return asd*2;
}

function somethingElse(double asd ){
    return (asd - (asd / 2));
}